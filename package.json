{
  "name": "ts-playground",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "serve": "vite --open",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages"
  },
  "devDependencies": {
    "gh-pages": "6.3.0",
    "typescript": "5.8.3",
    "vite": "7.1.2"
  }
}
